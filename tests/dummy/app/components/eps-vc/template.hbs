<PowerSelect
  @search={{false}}
  @searchEnabled={{true}}
  @searchField="name"
  @options={{options}}
  @selected={{selected}}
  @onchange={{action "onchange"}} as |usernames|>
  {{usernames.name}}
</PowerSelect>