{{#if searchEnabled}}
  <div class="ember-power-select-search">
    <span class="search-container" role="presentation">
      {{svg-jar "search" class="ember-svg size-14" role="presentation"}}
    </span>
    <input
      type="search"
      autocomplete="off"
      autocorrect="off" 
      autocapitalize="off"
      spellcheck="false"
      class="ember-power-select-search-input"
      value={{select.searchText}}
      role="combobox"
      aria-autocomplete="list"
      aria-haspopup="true"
      aria-expanded={{if dropdown.isOpen "true" "false"}}
      aria-activedescendant={{ariaActivedescendant}}
      aria-controls={{listboxId}}
      placeholder={{or searchPlaceholder null}}
      oninput={{onInput}}
      onfocus={{onFocus}}
      onblur={{onBlur}}
      onkeydown={{action "onKeydown"}}>
  </div>
{{/if}}